/// Personal keys for THIS machine only.
///
/// The copy on GitHub is intentionally empty so the public repo stays clean.
/// On your PC, fill these in — then run:
///   git update-index --skip-worktree lib/config/local_secrets.dart
/// so your keys never get committed.
class LocalSecrets {
  const LocalSecrets._();

  static const geminiApiKey = '';
  static const googleWebClientId = '';
}
